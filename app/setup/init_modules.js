(function () {
    'use strict';

    /* Modules */
    angular.module(globals.appName + globals.controllers, []);
    angular.module(globals.appName + globals.services, []);
    angular.module(globals.appName + globals.directives, []);
    angular.module(globals.appName + globals.filters, []);
    angular.module(globals.appName + globals.repositories, []);
    angular.module(globals.appName + globals.constants, []);
}());